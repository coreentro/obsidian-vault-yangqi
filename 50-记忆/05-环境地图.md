---
created: 2026-08-17
updated: 2026-08-17
type: 环境
tags:
  - 记忆
  - 环境
---

# 🗺️ 环境地图

> 「东西在哪」的速查表。**不含任何密钥**——只记变量名与所在文件。

## 💻 主机

| 项 | 值 |
|---|---|
| 系统 | macOS 26.6.1 |
| 用户目录 | `/Users/yangqi` |
| Python | `python3 3.11.15`（PEP 668：**必须用 venv 或 uv**，不能直接 pip 装全局）|
| 用户级 bin | `~/.local/bin`（已在 PATH）|

## 📁 关键路径

```text
知识库（唯一有效）
  ~/Library/Mobile Documents/iCloud~md~obsidian/Documents/yangqi
  ⚠️ Documents/文稿 - …/Obsidian Vault 是空的次级库，不要动

Hermes          ~/.hermes/            （config.yaml · .env · skills/ · plugins/ · cron/）
Obsidian 配置    ~/Library/Application Support/obsidian/obsidian.json
DeepSeek Harness ~/.dsh/              · App: ~/Applications/DeepSeek Harness.app
Codex Desktop    ~/.codex/            · CLI 独占: ~/.codex-cli/
Claude Code      /Users/yangqi/.local/bin/claude
Grok 注册器      ~/.grok-register/ · ~/Grok-Register/   （勿动 ~/.grok）
lark-cli         ~/.local/bin/
```

## 🌐 网络

- Shadowrocket：`utun` + fake-IP（`198.18.x.x`），本地代理 `127.0.0.1:1082`
- 路由分组：`Proxy | Config | Direct | Scene`
- 规则口径：`Hermes` 配置 = `FINAL,PROXY` + CN 直连；`Basic*` = `FINAL,DIRECT`
- 生效判据：plist 的 `CurrentRuleFileName`（**常仍是 `Basic.db`**，要确认三键一致）
- 本地 NewAPI 网关：`:3001`

## 🔑 凭据存放位置（只记位置，不记值）

| 用途 | 位置 |
|---|---|
| Hermes 各供应商密钥 | `~/.hermes/.env`，在 `config.yaml` 用 `key_env` 引用 |
| DeepSeek | 环境变量 `DEEPSEEK_API_KEY` |
| xAI / Grok | `hermes auth add xai-oauth`（OAuth，非明文 key）|
| 飞书 | lark-cli 自有 profile 存储 |

> 🚫 任何真实 key / token / cookie / 密码**都不写进知识库**。
