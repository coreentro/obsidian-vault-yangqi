# Implementation Log

## 2026-07-14

- Created the required conversation directory.
- Recorded the approved system design and exact data contracts.
- Confirmed that the workspace is not a Git repository; no commits will be created.
- Confirmed the current Mac has limited free space, so local staging must remain bounded.
- Confirmed Google Drive desktop, `ffmpeg`, `yt-dlp`, and Whisper are not yet installed.
- Confirmed `lark-cli`, SQLite, Python, and Coze CLI are available; Coze CLI authentication is active.
- Began test-first implementation of the append-only local archive core.
- Implemented immutable evidence packages, checksum manifests, lossless transcript validation, append-only audit events, exact source reconciliation, a read-only local inventory scanner, Chromium bookmark/history inventory, and a locally rebuildable full-text index.
- Froze the current Mac local-file baseline at 36,138 files without changing any source file. All items remain blocked pending value classification and complete evidence capture.
- Froze six Chromium-family browser profiles: 237 bookmarks and 6,801 history rows. Bookmarks are registered as strong-save signals; 6,054 readable history candidates remain blocked for value review.
- Created the Feishu Base `个人知识资产控制台` with device, source, candidate, asset, review-queue, and question-answer tables.
- Seeded and verified one device record and thirteen source records in Feishu. Every source obeys `scanned = included + excluded + blocked`; blocked rows remain visibly incomplete.
- Attempted to create an isolated Coze application project. Coze rejected creation because the account had insufficient credits, so no project or deployment was created and the local implementation continued.
- Downloaded the official Google Drive for desktop installer to `/Users/yangqi/Downloads/GoogleDrive.dmg`; verified its disk checksum, Apple notarization, and Google LLC Developer ID signature. Installation awaits the required immediate confirmation.
- Tried both user and application identities for Feishu hot-storage folder creation. The user token lacks `space:folder:create`; the application has not applied for `drive:drive` or `space:folder:create`. No folder was created.
- Installed a pinned media toolchain: `yt-dlp==2026.7.4`, `openai-whisper==20250625`, and Homebrew `ffmpeg==8.1.2`. The isolated environment and package cache occupy about 1.48 GB; no model weights were downloaded.
- Ran the complete automated suite with resource warnings promoted to errors: 21 tests passed and 0 failed.
- Reconciled the local ledger and Feishu control plane independently: 43,176 scanned, 237 included, 747 excluded, 42,192 blocked, and zero reconciliation delta.
- Confirmed the downloaded Google Drive payload contains a signed XAR distribution package with Google LLC Developer ID Installer certificates and Apple Silicon package version 127.0.1.0. Staged installer: `GoogleDrive-installer.pkg`.
- Attempted system installation; the package was recognized, but the automation sandbox cannot access the root installation volume. No application was installed or claimed installed.
- Attempted Feishu scope login; the sandbox cannot access the macOS Keychain (`keychain not initialized`). No new permission was granted and no hot-storage folder was created.
- Re-verified Feishu user authorization after the user resolved the local environment issue: `space:folder:create` and `drive:drive` are now present. Created the private hot-storage root and three child folders: `01-indexes`, `02-selected-knowledge`, and `03-evidence-manifests`.
- Re-verified Google Drive installation: `/Applications/Google Drive.app` is present, signed by Google LLC Developer ID, version 127.0.1.0, and the process is running. DriveFS logs still show `user_email: ""`, `current_account_token: ""`, and `no_user`; account sign-in and streaming readiness are therefore not yet verified.
- Completed Feishu upload authorization for `drive:file:upload` and `drive:drive.metadata:readonly` through device flow. Uploaded seven index/control artifacts and seven frozen inventory manifests; the destination folder listings independently show 7 and 7 files with no upload failures.
- Verified Google Drive account sign-in as `yangqihello@gmail.com`; the File Provider domain is writable and `fileproviderctl` reports the staged files as uploaded. Created the Google Drive evidence-root directory tree and copied 14 index, audit, and frozen-manifest files without changing local sources.
- Compared every staged local/cloud copy: 14 of 14 SHA-256 hashes and file sizes match exactly.
- Because the audit and implementation records changed after their first upload, versioned cloud copies `audit-report-2026-07-14-verified.md` and `implementation-log-2026-07-14-verified.md` will be added without overwriting the earlier records.
- Added append-only candidate import builders for browser bookmarks/history and local files. Explicit bookmarks enter the included queue; readable history enters review; filtered history remains excluded with a reason; unclassified local files remain blocked with path, size, modification time, MIME type, and SHA-256 preserved as signals.
- Added conflict-safe batch candidate registration with SQLite-safe chunks. Replaying an identical inventory is a no-op; a conflicting record never overwrites the frozen candidate.
- Seeded all seven frozen inventories into the local ledger: 43,176 candidate rows, comprising 237 included, 747 excluded, 6,054 review, and 36,138 blocked. Source reconciliation remains exact with delta zero.
- Added `data/candidate-queue-summary-2026-07-14.json` as a derived audit summary. The immutable inventory manifests remain the source of truth.
- Synchronized the candidate summary and 31 MB candidate ledger snapshot to Feishu `01-indexes` and Google Drive `00-indexes` with versioned names. Feishu multipart upload completed successfully; local and Google Drive SHA-256 values match (`c0fa9b...d802` for the summary and `38f68e...42bc4` for the ledger).
- Uploaded a versioned Feishu control-plane snapshot `feishu-control-plane-candidates-2026-07-14.json` and recorded its token in the local control-plane map; no earlier control-plane snapshot was overwritten.
- Frozen `data/capture-batch-001-2026-07-14.json` with 20 public knowledge-page bookmarks for the first evidence-package trial. The batch manifest was uploaded to Feishu (`Id65bzSpMocGjnxxLMZcGqTOnKf`) and Google Drive; its SHA-256 is `15df8eacbf92f388948ec9c44bdf471c9e9584bd00fe1206f6cde23ef4a4922c`.
- Checked the first two trial URLs in the in-app browser: one redirected to an untrusted tracking domain and another resolved to a deleted blog. Neither was captured as source content; these limitations will be recorded in capture logs instead of treating redirects as originals.

## 2026-07-15 — continuation

- Ran the frozen 20-item public-bookmark trial through the read-only fetcher using the installed Mozilla CA bundle for normal TLS verification; certificate validation was not disabled.
- Created 20 independent evidence packages under `data/evidence/`: 12 complete, 2 limited (raw application shell saved, visible article body absent), and 6 blocked (403/500/代理或 TLS 失败). No blocked or limited response was filled by inference.
- Registered all 20 trial assets in the local ledger. Asset completeness matches the package manifests exactly: 12 complete, 2 limited, 6 blocked. The original 237 bookmark inclusion count remains unchanged; this batch is only a first capture trial.
- Wrote `data/capture-results-001-2026-07-15.json` (SHA-256 `a5d946db463643fa9d7b6422aa2c4e94bbd5e0f9e93f57f2332a10f0d4cc9e00`) and `data/capture-verification-001-2026-07-15.json` (SHA-256 `2a1efcae503e47af256df893ba4fba55af0fc24658edeedd343d3a875fa03a3d`). The verification checks every package checksum and source/cloud copy.
- Copied the 20 packages (126 files, 4.3 MB) to Google Drive `personal-knowledge-assets/01-raw-evidence`; all 126 local/cloud SHA-256 comparisons match. File Provider currently reports `needs-auth: no`, `needs-indexing: no`, `pending-indexable-count: 0`.
- Added the result and verification manifests to Feishu `01-indexes` and Google Drive `05-capture-logs`; local control-plane metadata records the returned file tokens. Feishu Assets table now contains 20 review-required records, with valid Drive search links and explicit completeness/original-state fields.
- Re-ran source reconciliation after asset registration: 43,176 scanned, 237 included, 747 excluded, 42,192 blocked, delta 0. No local source file, browser database, bookmark, or platform record was moved or modified.
- Re-ran the complete automated suite with `ResourceWarning` promoted to errors: 28 tests passed and 0 failed.
- Restored 10 trial packages from the mounted Google Drive path into a temporary directory and rechecked each package's checksum list: 10/10 passed. The required broader random recovery sample remains a later acceptance gate.

## 2026-07-15 — second capture batch

- Frozen `data/capture-batch-002-2026-07-15.json` with 20 public knowledge-page bookmarks. Login, payment, personal dashboards, local-control URLs, and obvious utility pages were excluded from this batch.
- Created and registered 20 more immutable evidence packages: 13 complete, 4 limited, and 3 blocked. Blocked/limited pages retain their raw response or capture error and are not completed by model inference.
- Batch-002 result SHA-256: `8d8fa4171b11553dbe455bfa8090340d95a0e860e5cee64b4b238939ecd1f7b1`; verification SHA-256: `a64cb34e0420cb26f7eee3b24af15fbbab1b5a0ba5e80192f4c836b419716ddb`.
- Copied batch-002's 130 files to Google Drive `01-raw-evidence`; all local/cloud SHA-256 comparisons match. The combined evidence directory now contains 40 attempted assets, while the frozen included decision count remains 237.
- Added batch-002 manifests and results to Feishu `01-indexes` and Google Drive `05-capture-logs`; Feishu Assets now contains 40 review-required records.
- Source reconciliation remains unchanged at 43,176 scanned, 237 included, 747 excluded, 42,192 blocked, delta 0. No original source was moved, deleted, or edited.
- Restored 10 batch-002 packages from Google Drive and passed 10/10 checksum validations; combined recovery evidence now covers 20 trial packages.
- Corrected a derived aggregate transcription from `26 complete / 6 limited / 8 blocked` to the ledger- and batch-proven `25 complete / 6 limited / 9 blocked`. The original batch results and all evidence packages were unchanged.
- Verified the currently installed Google Drive application now reports version `128.0`; the earlier `127.0.1.0` installer and installation entries remain as historical records. The authenticated File Provider mount is active for `yangqihello@gmail.com`.
- Separated Feishu Base row counts from local SQLite counts after verification showed that the Feishu candidates, review, and answer tables are still empty. Local counts are now recorded independently, preventing local records from being misreported as uploaded Feishu rows.
- Generated a metadata-only classification for all 36,138 frozen local-file candidates without opening, moving, deleting, or changing source files. The derived result recommends 875 obvious metadata/generated/empty items for auditable exclusion and routes 35,263 items to review, including 242 sensitive-name items explicitly ineligible for automatic content capture.
- Added explainable value scoring for all 6,054 weak-signal browser-history candidates. The derived layer recommends 849 auditable exclusions, 5,158 reviews, and 47 high-score candidates; a conservative safety boundary overrides the score for session, social, payment, proxy, and other sensitive URLs.
- Added a local metadata-only review queue and answer contract. The queue now contains 41,314 append-only review records; no review output changes the frozen candidate or source evidence.
- Rebuilt the local search index with 25 complete source documents and added Chinese short-query fallback while preserving full indexed text.
- Implemented, tested, and independently reviewed the resumable public batch runner. It now pins verified public DNS results to the actual TCP connection, revalidates redirects, preserves exact `source-response.bin` bytes, rejects unsafe replay paths and symlinks, performs atomic ledger registration, and enforces network-capture budgets. Final independent review found no Critical or Important issues; 63 automated tests pass.
- Created 40 external `manifest-v2` correction sidecars for the legacy packages without changing their original file tree. The correction records explicitly disclose that the old format did not separately preserve exact raw HTTP response bytes; all 40 sidecars match the legacy package file hashes.
- Loaded all 43,176 candidates and 41,310 then 41,314 review records into Feishu Base in serial 200-row batches. A lost response after review batch 39 was reconciled from the actual server row boundary before resuming, avoiding duplicate insertion.
- Ran frozen batch 003 with four conservative public-page candidates. All four resolved to `198.18.0.x`, so the public-network boundary refused connection and created four blocked evidence packages with no invented source text. Replaying the same batch produced four `replayed` results and left the ledger at exactly 44 assets.
- Copied batch 003's 20 package files to Google Drive and verified all local/cloud hashes. Feishu Assets now has 44 rows and the four related candidate rows link to their new asset IDs.
- Located two unattempted bookmark records whose exact URLs already have complete evidence from another browser source. Created separate limited duplicate-reference packages preserving each frozen bookmark record, linked them through `duplicate_of`, and did not re-download or delete either source. The ledger and Feishu now contain 46 assets and 41,316 review rows; 191 bookmarks remain unattempted.
- Randomly selected 20 of 46 assets without replacement and restored each package from Google Drive into an isolated temporary directory. Local, cloud, and restored file sets, sizes, SHA-256 hashes, and package checksum lists passed 20/20; the temporary restore was removed after verification.
- Completed ten evidence-backed personal-question blind tests. Nine improved judgment or action, one correctly refused a current admissions conclusion because the stored evidence was stale/incomplete, and fabricated-source count was zero. All ten answers include coverage, conflicts, confidence boundaries, actions, and current-source requirements and are written to the local ledger and Feishu Answers table.
- Routed every one of the 191 remaining bookmarks into an explicit manual queue: 23 platform/session items, 12 private/account items, 34 sensitive-resource items, and 122 conservative unresolved items. All 191 records were appended to both the local and Feishu review queues; none authorizes automatic login, download, or capture.
- Continued the current-Mac local-content stage instead of waiting for another device. Froze a five-file, 15,433-byte read allowlist using path, extension, frozen size, and frozen SHA-256 only; sensitive-name files, credentials, private third-party material, generated dependencies, and non-allowlisted content remained unopened.
- Read the complete contents of the five allowlisted Markdown files. All five matched the frozen byte length and SHA-256, produced zero configured secret/identity pattern hits, and were accepted as reusable knowledge after full-content assessment. No optimization, shortening, or source rewrite was performed.
- Added a tested local-file evidence capturer that rejects symlinks, changed files, oversized inputs, invalid UTF-8, and conflicting replay. It stores the exact original bytes as `source-original.md`, an exact-decoding `source.md` derivative, metadata, scope-aware comments, capture log, manifest, and checksums. The suite now passes 71/71 tests.
- Captured and registered five complete local assets. Ledger counts are now 51 assets (30 complete, 8 limited, 13 blocked), 41,512 review records, and 30 searchable full-text documents. A second identical run added zero assets and changed no finalized package.
- Copied all five packages (35 files) and versioned database/index/audit artifacts to Google Drive. Local/cloud file sets and SHA-256 values match for every package file. Uploaded the same versioned indexes to Feishu; Feishu Base now has 51 assets and 41,512 reviews, and each of the five candidate rows points to its asset.
- Applied the PDF-specific workflow to a second five-file batch of chemistry literature. The allowlist contains 44,450,227 frozen bytes and excludes personal forms, admissions material, banking files, credentials, and non-scholarly PDFs before content access.
- Traversed all 62 PDF pages with `pypdf`; no page lacked extractable text. Rendered the first, middle, and last pages of each article with Poppler and visually checked 15 pages. Titles, abstracts, figures, equations, references, page orientation, and the Chinese abstract page were legible and unclipped.
- Added a PDF evidence path that retains exact `source-original.pdf` bytes, complete page-delimited `transcript-raw.txt`, a separate `source.md`, extraction scope, visual-review scope, metadata, comments scope, capture log, manifest, and checksums. Raster figure text remains governed by the untouched PDF rather than being guessed from extraction.
- Captured five complete chemistry PDFs and registered them in the ledger and search index. The current local totals are 56 assets (35 complete, 8 limited, 13 blocked), 41,517 reviews, and 35 search documents; 72 automated tests pass. Immediate replay added zero assets.
- Copied the five PDF packages (45 files) to Google Drive and verified every file hash. Uploaded versioned ledger/index/batch artifacts to Feishu, added five reviewed asset rows and five resolved review rows, and independently read back the final boundaries and candidate-to-asset links. Feishu now matches the local 56-asset and 41,517-review boundaries.
- Continued with chemistry PDF batch 002: froze and fully assessed five additional scholarly PDFs totaling 17,089,318 bytes, covering reusable magnesium-oxide condensation catalysis, continuous chalcone production kinetics, MoOx-modified ceria Michael addition, a robust chalcone synthesis protocol, and a systematic chalcone review.
- Traversed all 64 pages and visually checked the first, middle, and last page of each PDF (15 checks). Every exact original PDF remains the authority in its immutable evidence package; extracted text, metadata, aliases, and review notes are additive derivatives only.
- Captured five complete assets, replayed the batch idempotently with zero new assets, copied 45 package files to Google Drive with matching hashes, and synchronized versioned ledger/index/control artifacts. Local and Feishu boundaries now agree at 61 assets and 41,522 reviews; the local search index contains 40 documents and 15 bilingual alias records.
- A timestamp audit found that four derived batch-control files contained planned future time labels. The files were not rewritten: `data/local-pdf-batch-002-timestamp-correction-2026-07-15.json` records the filesystem and ledger ordering append-only. Original source evidence and asset/index content were unaffected.
- Continued with chemistry PDF batch 003: froze five non-sensitive scholarly files totaling 5,739,907 bytes and excluded personal certificates, admissions forms, examination documents, and other sensitive candidates before content access.
- Traversed all 45 pages and visually checked 15 pages. The articles cover aqueous microwave chalcone synthesis, low-pH ammonia-nitrogen removal, semiconductor core-shell photocatalysis, flavone synthesis teaching, and CeO2/CdxZn1-xS hydrogen evolution. All exact original PDF bytes remain immutable.
- Added explicit review-version labeling to the PDF capture script and an append-only alias-addition table. Frozen base alias sets are never replaced; five supplemental retrieval phrases were appended and verified without rewriting source text or prior aliases.
- Captured five complete assets, replayed with zero new assets, copied 45 package files and versioned ledger/search snapshots to Google Drive, and wrote five asset plus five review rows to Feishu. Feishu boundary reads and candidate links verify 66 assets and 41,527 reviews. The full suite now passes 75 tests.
- Continued with scholarly PDF batch 004: froze five non-sensitive files totaling 10,169,065 bytes, covering SEM instrumentation, hydrazine colorimetry, element-doped g-C3N4, general photocatalytic water splitting, and CdS modification.
- Traversed all 45 pages and visually checked 15. The SEM PDF has an embedded-font encoding defect that loses some Traditional Chinese glyphs in extraction and sampled rendering. The system records this limitation inside that asset's `capture-log.json`, does not infer the author or missing characters, and retains the exact PDF as authority.
- Captured five complete original-PDF packages, replayed with zero new assets, copied 45 files and versioned ledger/search snapshots to Google Drive, and synchronized five asset plus five review rows to Feishu. Feishu boundaries now verify 71 assets and 41,532 reviews; 75 tests and 20/20 original-byte comparisons pass.
- Added a separate bilingual alias table and trigram index for the ten local assets. Alias replays are idempotent, conflicting replacements are rejected, and the immutable document rows remain unchanged. Chinese queries for sulfur vacancies, ceria oxygen vacancies, mechanochemical Michael additions, specification-driven development, and Feishu course automation now place the intended assets first. The full suite passes 73 tests.

## 2026-07-15 — local index health check

- Added `scripts/verify_local_search_index.py`, a read-only SQLite health checker. It opens the existing search index in `mode=ro`, runs `PRAGMA integrity_check`, verifies every document has non-empty source text and an FTS row, and probes document/base-alias/append-only-alias recall. It writes a new report only and never edits the index or evidence packages.
- Verification passed: SQLite integrity `ok`, 55 indexed documents, 30 base-alias records, 15 append-only alias records, zero empty source texts, and zero documents missing FTS rows. Report: `data/local-index-verification-2026-07-15.json` (index SHA-256 `ef18fa5e55a8b491410de7e2e5da07a05ce7eebaccb374d62df00f38902d5a7a`).

## 2026-07-15 — status verification

- Re-read the append-only project status and verified the current boundary remains 1 device, 13 sources, 43,176 candidates, 71 assets, and 41,532 review records; no source files or evidence packages were changed.
- Ran the complete test suite with both the project root and `src/` on `PYTHONPATH`: 75 tests passed, 0 failed.
- The next ingestion step is intentionally gated on user-visible access to another device/account or an explicit manual decision for the 191 remaining bookmark records; the project is not labeled full-device or full-account complete.

## 2026-07-15 — batch 005 reconciliation and sync

- Reconciled the latest local ledger against the actual SQLite tables. Batch 005 adds five PDF assets; the authoritative current boundary is now 76 assets and 41,537 review records, with 54 complete, 9 limited, and 13 blocked assets. The earlier 71/41,532 figures remain historical snapshots.
- Verified all five batch-005 evidence packages against the mounted Google Drive copy: 9 files per package, 45/45 package files, and matching SHA-256 values.
- Uploaded batch-005 manifests, alias snapshot, search index, ledger snapshot, status v006, and sync verification v005 to the private Feishu index folder using versioned filenames. No earlier cloud artifact was overwritten.
- Added five asset rows and five resolved review rows to the Feishu control Base. The rows preserve the limited extraction boundary for `asset-5ef1ee75c15e6bffb2c9b908`; no missing text was inferred.
- Wrote `data/implementation-status-2026-07-15-v006.json` and `data/local-pdf-sync-verification-2026-07-15-v005.json` as append-only corrections. The project remains active with explicit access blockers; it is not marked full-device or full-account complete.

## 2026-07-15 — batch 006 continuation

- Selected two non-sensitive PDF candidates after metadata review and visual inspection: a one-page scholarly book review and a three-page SEM/EDS application note.
- Traversed both PDFs for extraction and visually checked all pages. The book review is complete; the SEM note is image-based and limited, with the original PDF retained as the only authority and no reconstructed text.
- Captured both packages with frozen SHA-256 and byte-length checks, then replayed the same batch. The first run added 2 assets; the replay added 0 and did not alter either package.
- Copied both packages to Google Drive with 18/18 file hashes matching, uploaded versioned batch manifests and status files to Feishu, and added 2 asset rows plus 2 resolved review rows to the Feishu Base.
- Current authoritative boundary is 78 assets (55 complete, 10 limited, 13 blocked) and 41,539 review records, recorded in `data/implementation-status-2026-07-15-v007.json` and `data/local-pdf-sync-verification-2026-07-15-v006.json`.
- Final post-batch index verification found 57 searchable documents, 30 base aliases, and 15 append-only aliases with SQLite integrity `ok`, zero empty source texts, and zero documents missing FTS rows. This correction is recorded as `data/implementation-status-2026-07-15-v008.json` and `data/local-index-verification-2026-07-15-v003.json`; both were synced to Google Drive and Feishu without overwriting earlier versions. The complete 75-test suite remains green.
